#include <stdio.h>

int main () {
    int arr[31], a;
    int f[31];

    for (int i=1; i<=30; i++) {
        arr[i]=i;
    }
    scanf("%d", &a);
    for (int j=1; j<=30; j++) {
        if (arr[j]!=a) {

                }
    }
    printf("%s", f);

    return 0;
}